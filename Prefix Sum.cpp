#include<iostream>
#define MAX 20
using namespace std;

int main()
{
    int arr[MAX], temp_arr[MAX], i, size;
    
    cout << "Enter size for the array : ";
    cin >> size;
    
    cout << "Enter " << size << " elements for the array : ";
    for (int i = 0; i < size; ++i) {
        cin >> arr[i];
    }

    temp_arr[0] = arr[0];
    for ( i = 1; i < size; i++)
    {
        
    }
    
}